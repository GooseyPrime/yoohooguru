# Main App Environment Variables
# This file should be created locally and not committed to version control

# NextAuth Configuration
# App-specific URL
NEXTAUTH_URL=https://www.yoohoo.guru

# Google OAuth Credentials
# Get these from Google Cloud Console
GOOGLE_OAUTH_CLIENT_ID=your_google_oauth_client_id_here
GOOGLE_OAUTH_CLIENT_SECRET=your_google_oauth_client_secret_here

# Analytics Configuration
# Get these from Google Analytics and Google Tag Manager
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX
NEXT_PUBLIC_GTM_CONTAINER_ID=GTM-XXXXXXX

# Vercel Analytics (automatically enabled on Vercel)
NEXT_PUBLIC_VERCEL_ANALYTICS=true

# Note: The following are shared across all apps via .env.shared:
# - NEXTAUTH_SECRET (shared secret for JWT signing)
# - AUTH_COOKIE_DOMAIN=.yoohoo.guru (enables cross-subdomain cookies)
