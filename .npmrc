# NPM configuration for monorepo workspace
# Ensure dependencies are properly installed for workspaces
# Note: Using legacy-peer-deps due to React 19 / Next.js 14 compatibility
legacy-peer-deps=true
fund=false
audit=false
# Enable workspace protocol for better dependency resolution
workspaces-update=true
